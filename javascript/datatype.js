"use strict" // treat all js code to use newer version 
let num =123456;
let text = "sushnika";
// alert(3+3); // not suitalbe for node js works in browser

console.log(typeof num);
console.table([typeof num, typeof text]);
console.log(num);